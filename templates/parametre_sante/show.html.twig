<!DOCTYPE html>

<title>ParametreSante</title>

{% block body %}
    <h1>ParametreSante</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ parametre_sante.id }}</td>
            </tr>
            <tr>
                <th>DateMesure</th>
                <td>{{ parametre_sante.dateMesure ? parametre_sante.dateMesure|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Valeur</th>
                <td>{{ parametre_sante.valeur }}</td>
            </tr>
            <tr>
                <th>Note</th>
                <td>{{ parametre_sante.note }}</td>
            </tr>
            <tr>
                <th>Typeparametre</th>
                <td>{{ parametre_sante.typeparametre }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_parametre_sante_index') }}">back to list</a>

    <a href="{{ path('app_parametre_sante_edit', {'id': parametre_sante.id}) }}">edit</a>

    {{ include('parametre_sante/_delete_form.html.twig') }}
{% endblock %}
